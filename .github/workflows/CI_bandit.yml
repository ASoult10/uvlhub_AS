name: Bandit CI

on:
  push:
    branches: [main, trunk, feature_task_ci]
  pull_request:
    branches: [main, trunk, feature_task_ci]

jobs:
  bandit-scan:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install bandit

      - name: Run Bandit security analysis
        run: |
          bandit -r app/ -ll -o bandit-report.txt -f txt

      # Muestra el contenido del informe en los logs (opcional, pero útil)
      - name: Show Bandit report
        if: failure()
        run: cat bandit-report.txt

      # Sube el informe como artefacto SOLO si el análisis falla
      - name: Upload Bandit report (only on failure)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: bandit-security-report
          path: bandit-report.txt
